
function range(x, y) {
    let list = [];
    for (let i = x; i<=y; i++) {
        list.push(i)
    }
    return list;
}

function sum(...numbers) {
    let result = numbers[i];
    for (let i of numbers) {
        if (numbers > result) result = number;
    }
    return result;
}

console.log(sum(range(1,10)));